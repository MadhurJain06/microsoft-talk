# microsoft-workshop
### this whole react interface is the output of my implications of github-copilot codespaces, using copilot as agent.

## User interface:
![image](https://github.com/user-attachments/assets/a7570546-71e5-4be9-8eae-a7d7695976cf)
## Features:
it allows user to toggle/ change the memes which are accessed from apen apis
user can add these as favourites and see them under a column.
Also, the user can edit the memes with text, (lower and above text as general in memes) as well as one can also drag the text to different locations inside the image.
It also give feature to save these edited images in the device.
